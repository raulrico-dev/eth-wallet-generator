import './generator.css';

export default function GeneratorButton(params) {
    return (
        <button onClick={""} className="btn generator-btn">
            {"Generate"}
        </button>
    )
}